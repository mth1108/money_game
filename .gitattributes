# 줄바꿈 정규화
# core.autocrlf=true 환경에서 셸 스크립트가 CRLF 로 체크아웃되면
# 리눅스·CI 에서 실행이 깨진다. 아래 파일은 LF 로 고정한다.
*.sh     text eol=lf
gradlew  text eol=lf

# 윈도우 배치 파일은 CRLF 유지
*.bat    text eol=crlf
*.cmd    text eol=crlf
