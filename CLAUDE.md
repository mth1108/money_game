# 머니게임 — 개발 명세

> **이 문서의 용도**
> 코딩 에이전트(Claude Code 등)와 함께 개발할 때 컨텍스트로 쓰는 문서입니다.
> 작업을 시작하기 전에 **§1 절대 규칙**과 해당 모듈 항목을 먼저 읽고 진행합니다.
> 구조는 의도적으로 느슨하게 잡았습니다. 세부 시그니처와 필드는 구현하면서 바뀌는 것을 전제로 합니다.

---

## 0. 프로젝트 한 줄 요약

여러 명이 같은 방에서 **실제 과거 시세를 압축 재생한 구간**을 거래하고, 5분 뒤 남은 자산으로 순위를 가리는 웹게임. 배율(레버리지)과 청산이 있습니다.

**스택** — React / Spring Boot / MySQL / WebSocket / 토스증권 Open API
**기간** — 약 4개월, 개인 프로젝트

---

## 1. 절대 규칙 (가드레일)

**아래 규칙을 깨는 코드는 작성하지 않습니다. 규칙과 충돌하는 요청을 받으면 먼저 지적하고 확인을 구합니다.**

### 1.1 실계좌 보호

- 토스 API의 **주문·계좌·잔고 관련 엔드포인트를 호출하는 코드를 어떤 이유로도 작성하지 않습니다.**
- 시세 수집 모듈에는 시세 조회 외의 API 클라이언트 코드를 두지 않습니다.
- 토큰 하나로 실제 주문이 나갈 수 있습니다. 이 규칙은 타협 대상이 아닙니다.

**금지 엔드포인트** — OpenAPI 스펙 v1.2.15에서 확인한 실명입니다 (2026-09-14).

```
POST   /api/v1/orders                        주문 생성
POST   /api/v1/orders/{orderId}/cancel       주문 취소
POST   /api/v1/orders/{orderId}/modify       주문 정정
GET    /api/v1/orders  /api/v1/orders/{id}   주문 조회
ALL    /api/v1/conditional-orders*           조건주문 전체
GET    /api/v1/accounts                      계좌 목록
GET    /api/v1/holdings                      보유 주식
GET    /api/v1/buying-power                  매수 가능 금액
GET    /api/v1/sellable-quantity             판매 가능 수량
GET    /api/v1/commissions                   매매 수수료
WS     personal:order                        본인 주문 이벤트 채널
```

**사용 엔드포인트** — 이 프로젝트가 호출하는 것은 아래뿐입니다.

```
POST   /oauth2/token                         토큰 발급
GET    /api/v1/candles                       캔들 조회  ← 사실상 이것 하나
GET    /api/v1/stocks                        종목 기본 정보 (결과 화면의 종목명 공개용)
```

### 1.2 게임 진행 중 I/O 금지

- 틱 루프가 도는 동안 **DB 쓰기를 하지 않습니다.**
- 틱 루프가 도는 동안 **외부 API를 호출하지 않습니다.**
- DB 접근은 ① 판 시작 시 시세·시나리오 읽기 ② 판 종료 시 결과 쓰기, 두 순간뿐입니다.

### 1.3 만들지 않는 테이블

현재 상태를 저장하는 테이블은 만들지 않습니다.

- `user_balance`, `holdings`, `current_positions` 등 **진행 중 상태 테이블 금지**
- 이런 테이블이 생기면 매 거래마다 UPDATE가 발생하고, 설계 전체가 무너집니다
- 진행 중 상태는 서버 메모리의 `GameSession` 객체에만 존재합니다

### 1.4 만들지 않는 기능

- 호가창, 지정가 주문, 부분 체결, 미체결 큐, 매칭 엔진
- 공매도 (데이터 구조에 `side` 필드는 두되, 로직은 LONG만 구현)
- 서버 장애 시 진행 중 게임 복구
- 모바일 네이티브 앱

### 1.5 금액 계산

- 모든 금액·가격은 `BigDecimal` 사용. **`double`/`float` 금지**
- DB 컬럼은 `DECIMAL`
- 수량은 정수

**수량과 증거금 — 정수 수량, 증거금 역산**

```
quantity      = floor( (margin x leverage) / entryPrice )
actualMargin  = (quantity x entryPrice) / leverage
cash         -= actualMargin + fee
```

- 사용자가 입력한 `margin` 은 **상한**입니다. 실제 차감액은 체결 수량에서 역산합니다
- 잔돈을 돌려주는 것이 아니라 **애초에 그만큼만 씁니다**
- 이유: `actualMargin x leverage == quantity x entryPrice` 가 정확히 성립해야
  청산가 계산에 오차가 생기지 않습니다. 잔돈 반환 방식은 margin 과 실제 포지션
  크기가 어긋난 채 남아 청산 계산이 틀어집니다
- `quantity == 0` 이면 주문 거부. 사유는 「증거금 부족」

**scale 과 RoundingMode**

| 대상 | scale | RoundingMode | 이유 |
|---|---|---|---|
| 중간 계산 | 8 | `HALF_UP` | 나눗셈은 전부 `divide(x, 8, HALF_UP)` |
| 최종 가격 | 4 | 용도별 | 최종 단계에서만 적용 |
| 청산가 | 4 | `DOWN` | 내림 → 늦게 청산 → 플레이어에 유리 |
| 수수료 | 4 | `UP` | 올림 → 잔고 음수 방지 |

- **중간마다 반올림하지 않습니다.** 최종 단계에서만 적용합니다
- 원칙: **애매하면 플레이어에게 유리하게, 단 잔고는 보수적으로**
- ⚠️ **이 원칙은 반올림 방향처럼 플레이어가 통제할 수 없는 계산 오차에만 적용합니다.**
  게임 규칙 자체에는 적용하지 않습니다. 규칙에 적용하면 특정 플레이 방식에 보너스를
  주게 되고, 그러면 게임이 요구하는 의사결정 자체가 사라집니다

**수수료 정책**

```
진입 1회 + 종료 1회. 종료 사유는 불문한다.
```

| 시점 | 수수료 | 기준가 |
|---|---|---|
| 진입 (매수) | 부과 | 진입가 |
| 정상 청산 (매도) | 부과 | 매도 시점 현재가 |
| 강제 청산 (liquidation) | 부과 | 청산된 틱의 현재가 |
| 판 종료 시 정리 | 부과 | 마지막 틱의 현재가 |

- **면제 구간을 두지 않습니다.** 종료 정리만 면제하면 239틱에 파는 쪽이 수수료를
  더 내게 되어, 합리적인 플레이어는 절대 손절하지 않게 됩니다. 배율과 청산을 넣은
  목적이 「팔지 말지 고민하게 만드는 것」인데 그 고민 자체가 사라집니다
- 싱글 모드 상대인 존버 봇(M9 `HoldBot`)이 구조적으로 유리해지는 것도 같은 원인입니다
- 다만 **수수료는 잔여 현금을 초과해 부과하지 않습니다.** 잔고 불변식은 §3 M5 참조

**청산 판정**

```
현재가 <= liquidationPrice  →  청산   (등호 포함)
```

**경계값 테스트 (필수)**

배율 3(`1/3` 이 무한소수)으로 반드시 케이스를 만듭니다.
배율 2로만 테스트하면 이 문제를 잡지 못합니다.

- `liquidationPrice + 0.0001` → 생존
- `liquidationPrice`          → 청산
- `liquidationPrice - 0.0001` → 청산

### 1.6 동시성

- **한 방(room)의 상태 변경은 반드시 직렬로** 처리합니다
- 틱 진행과 주문 처리가 같은 실행 흐름 안에서 순서대로 일어나야 합니다
- 방마다 독립적이므로 방 간 락은 필요 없습니다

### 1.7 작업 범위

- 지시받은 모듈 밖의 코드를 임의로 리팩터링하지 않습니다
- 모듈 경계(§3의 「하지 않는 일」)를 넘어가지 않습니다
- 새 의존성을 추가하기 전에 확인을 구합니다

### 1.8 개발 순서

- **프론트엔드를 먼저 다듬지 않습니다.** M4(게임 엔진)가 테스트로 검증되기 전까지 화면은 디버그용 최소 구현만 합니다
- 엔진 모듈은 **단위 테스트 없이 다음 단계로 넘어가지 않습니다**

---

## 2. 시스템 구성

### 2.1 런타임 구조

```
 [브라우저]                    [게임 서버]                 [DB]
  React            REST  ──▶  RoomController          ┌──────────┐
    │                          ResultController  ──▶  │ 시세     │
    │              WS    ◀─▶  GameSocketHandler       │ 시나리오  │
    │                              │                   │ 결과     │
    │                         GameSession(메모리)  ──▶ │ 거래내역  │
    └─────────────────────────────────────────────────└──────────┘
                                                            ▲
                                              [시세 수집기] ─┘
                                              (별도 실행, 배치)
```

**핵심** — 시세 수집기는 게임 서버와 분리된 프로세스/커맨드입니다. 게임 서버는 외부 API를 모릅니다.

### 2.2 저장소 구조

Git 저장소는 **하나**입니다. 그 안에 백엔드와 프론트엔드가 각각 독립적인 프로젝트 루트로 들어갑니다.

```
moneygame/
├── .git/
├── .gitignore                 ← 두 프로젝트 것을 합쳐서 루트에 하나
├── README.md
├── CLAUDE.md                  ← 이 문서
│
├── backend/                   ← Spring Boot 프로젝트 루트
│   ├── build.gradle
│   ├── gradlew
│   └── src/
│       ├── main/
│       │   ├── java/com/moneygame/
│       │   │   ├── MoneygameApplication.java
│       │   │   │
│       │   │   ├── collector/          ← M1 시세 수집기
│       │   │   │   ├── TossApiClient.java
│       │   │   │   ├── TossAuthService.java
│       │   │   │   ├── CandleCollectService.java
│       │   │   │   └── CollectCommand.java        (CommandLineRunner)
│       │   │   │
│       │   │   ├── marketdata/         ← M2 시세 저장소
│       │   │   │   ├── PriceDataProvider.java     (인터페이스)
│       │   │   │   ├── DbPriceDataProvider.java
│       │   │   │   ├── CsvPriceDataProvider.java
│       │   │   │   └── entity/
│       │   │   │
│       │   │   ├── scenario/           ← M3 시나리오
│       │   │   │
│       │   │   ├── engine/             ← M4 게임 엔진 ★
│       │   │   │   ├── GameSession.java
│       │   │   │   ├── TickResult.java
│       │   │   │   ├── OrderRequest.java
│       │   │   │   ├── OrderResult.java
│       │   │   │   └── GameResult.java
│       │   │   │
│       │   │   ├── position/           ← M5 포지션·체결·청산
│       │   │   │   ├── Position.java
│       │   │   │   ├── PlayerState.java
│       │   │   │   └── LiquidationRule.java
│       │   │   │
│       │   │   ├── room/               ← M6 방 관리
│       │   │   │   ├── RoomService.java
│       │   │   │   ├── GameLoopRunner.java        (스케줄러)
│       │   │   │   └── RoomController.java
│       │   │   │
│       │   │   ├── realtime/           ← M7 실시간 통신
│       │   │   │   ├── GameSocketHandler.java
│       │   │   │   └── message/
│       │   │   │
│       │   │   ├── result/             ← M8 결과·전적
│       │   │   │
│       │   │   └── bot/                ← M9 봇
│       │   │       └── HoldBot.java
│       │   │
│       │   └── resources/
│       │       ├── application.yml
│       │       ├── application-local.yml          ← API 키. Git 제외
│       │       └── db/schema.sql                  ← 시세 테이블 DDL 수동 작성
│       │
│       └── test/java/com/moneygame/
│           ├── engine/GameSessionTest.java        ← S2의 완료 판정
│           └── position/LiquidationTest.java
│
├── frontend/                  ← React 프로젝트 루트 (S3에서 생성)
│   ├── package.json
│   └── src/
│       ├── api/               REST 호출
│       ├── socket/            WebSocket 연결·메시지 핸들링
│       ├── pages/             방 목록 / 대기실 / 게임 / 결과
│       └── components/
│
└── data/                      ← 초기 개발용 CSV. Git 제외 권장
    └── samples/
```

**구조 규칙**

- `backend/`와 `frontend/`는 서로 모릅니다. 각각 독립적으로 빌드·실행됩니다
- 패키지는 **계층(controller/service/repository)이 아니라 모듈 단위**로 나눕니다. §3의 모듈 경계와 디렉토리가 1:1로 대응해야 어디를 건드릴지 명확해집니다
- `engine/`과 `position/`은 **Spring 어노테이션과 JPA 엔티티를 참조하지 않습니다** (§1 참조). 순수 자바 객체만 둡니다

**Git 제외 대상**

```gitignore
build/
.gradle/
node_modules/
dist/
application-local.yml
.env
data/
```

> **API 키가 들어간 파일은 반드시 제외합니다.** 토스 키는 실계좌 주문까지 열리는 자격증명입니다. 커밋 전 `git status`로 확인하는 습관을 들입니다.

### 2.3 실행 단위

하나의 Spring Boot 애플리케이션이지만 **두 가지 방식으로 실행**됩니다.

| 실행 | 방식 | 언제 |
|---|---|---|
| 시세 수집 | `--spring.profiles.active=collector` 로 배치 실행 후 종료 | 필요할 때 수동 |
| 게임 서버 | 기본 프로파일. 웹 서버로 상주 | 상시 |

수집기를 별도 프로젝트로 분리하지 않는 이유는 엔티티와 설정을 공유하기 때문입니다. 다만 **프로파일로 실행 경로를 나눠 게임 서버가 수집 코드를 절대 타지 않도록** 합니다.

---

## 3. 백엔드 모듈

각 모듈은 **책임 / 하는 일 / 하지 않는 일 / 완료 판정**으로 정의합니다.
필드명과 메서드명은 제안이며, 구현하면서 조정할 수 있습니다.

---

### M1. 시세 수집기 `market-collector`

**책임** — 토스 Open API에서 캔들을 받아 DB에 적재한다. 게임 서버와 독립적으로 실행된다.

**하는 일**
- OAuth2 client_credentials로 액세스 토큰 발급 및 갱신
- 종목 코드 + 기간 + 단위(일봉/1분봉)를 받아 캔들 조회
- 요청당 200봉 제한 → `before` 파라미터로 페이지네이션
- 수정주가 적용 옵션 사용
- 이미 DB에 있는 구간은 건너뛰고 결손 구간만 호출
- 실행 결과를 로그로 남김 (몇 종목, 몇 건, 실패 여부)

**확정 API 사양** — OpenAPI 스펙 v1.2.15 기준 (2026-09-14 확인)

`GET /api/v1/candles`

| 파라미터 | 값 |
|---|---|
| `symbol` | 필수. KRX 6자리(`005930`) 또는 US 티커 |
| `interval` | 필수. **`1m`, `1d` 두 개뿐.** 5분봉·시간봉은 제공하지 않음 |
| `count` | 최대 200, 기본 100 |
| `before` | ISO 8601, **inclusive**. 응답의 `nextBefore`를 그대로 넘긴다 |
| `adjusted` | 기본 `true` (수정주가) |

- 응답은 `{ candles[], nextBefore }`. **`nextBefore`가 `null`이면 마지막 페이지**
- 가격·거래량은 `string` + `format: decimal` (`"71600"`). 그대로 `BigDecimal`로 파싱 (§1.5)
- **응답 정렬은 최신순(내림차순).** 오름차순 변환 책임은 M2에 있다
- `timestamp` 의미가 단위마다 다르다
  - `1m` — **봉 종료 시각.** 구간은 `[timestamp − 1분, timestamp)`
  - `1d` — 거래일 (시각은 현지 자정 고정)
- Rate limit `MARKET_DATA_CHART` — **초당 20회가 아니라 토큰 버킷이다**
  - `X-RateLimit-Limit` 은 버스트 용량, `X-RateLimit-Remaining` 은 남은 토큰,
    `X-RateLimit-Reset` 은 토큰 1개가 재충전되기까지의 초
  - 평균 1.35 TPS 로 돌려도 267 요청 연속이면 버킷이 말라 429 가 난다 (실측)
  - 요청 간 0.3초를 두면 잔량이 19/20 로 유지된다 (실측, 1800요청 무사고)
  - 429 수신 시 `Retry-After` + 지수 백오프. **수집기 필수 구현 사항**

**토큰 제약** — `POST /oauth2/token`

- `application/x-www-form-urlencoded`, `grant_type=client_credentials`
- refresh token 없음. 만료되면 같은 엔드포인트로 재발급 (`expires_in` 예시 86400초)
- ⚠️ **client당 유효한 토큰은 1개. 재발급하면 이전 토큰이 즉시 무효화된다**
  - `TossAuthService`는 토큰을 캐시해 재사용하고 만료 직전에만 재발급할 것
  - 수집기를 동시에 두 개 띄우면 서로의 토큰을 죽인다
  - 무효화된 토큰으로 호출하면 `token-revoked` 가 돌아온다.
    실측 중 1분봉 수집이 608요청째에 이걸로 끊겼다 (일봉을 동시에 돌린 탓)
- Rate limit `AUTH` = 초당 5회

**하지 않는 일**
- 게임 로직 관련 어떤 것도
- 주문·계좌 API 호출 (§1.1)

**완료 판정**
- 커맨드 한 줄로 종목 N개의 지정 기간 캔들이 DB에 들어간다
- 같은 명령을 두 번 실행해도 중복 행이 생기지 않는다

**메모**
- **조회 범위 실측 결과** — 2026-09-14, 005930(삼성전자), `scripts/probe-candle-range.sh`

| 단위 | 가장 오래된 봉 | 확인된 범위 | 봉 수 | 상태 |
|---|---|---|---|---|
| `1d` | 1975-06-12 | **51년 3개월** | 13,735 | `nextBefore = null` — **경계 확정** |
| `1m` | 2024-05-27 이전 | **최소 2년 4개월** | 403,000+ | 측정 중단. 경계 미도달 |

- 문서에는 보존 기간 언급이 전혀 없다. 위 수치는 전부 실측이다
- **"1분봉은 최근 수 주치만 남는다"는 통념은 틀렸다.** 최소 2년 이상 나온다
- 따라서 1분봉을 서둘러 쌓을 필요가 없다. 시나리오 구간은 나중에도 다시 받을 수 있다
- 한 종목의 1분봉 2년치는 약 40만 봉 / 2,000요청 / 40분이 든다.
  수집기는 전 구간을 훑지 말고 **시나리오에 필요한 구간만** 좁혀 받아야 한다
- 개발 초기에는 CSV를 읽는 구현으로 대체 가능 (§M2의 `PriceDataProvider`)

---

### M2. 시세 저장소 `market-data`

**책임** — 게임 서버가 시세를 읽는 유일한 통로.

**하는 일**
- 종목 + 기간 + 단위로 캔들 배열 조회
- 조회 결과를 시간 오름차순으로 정렬해 반환
  - ⚠️ 토스 API 응답은 **최신순(내림차순)**이다. 수집·조회 경로 어딘가에서 반드시 뒤집어야 한다

**인터페이스 (이건 고정)**

```java
public interface PriceDataProvider {
    List<Candle> getCandles(String symbolCode, Interval interval,
                            LocalDateTime from, LocalDateTime to);
}
```

- 구현체 1: `DbPriceDataProvider` (운영)
- 구현체 2: `CsvPriceDataProvider` (초기 개발용)

**하지 않는 일**
- 외부 API 직접 호출 (M1의 역할)
- 지표 계산

**완료 판정**
- 두 구현체를 갈아 끼워도 상위 코드가 바뀌지 않는다

---

### M3. 시나리오 `scenario`

**책임** — 한 판에 쓰이는 「종목 + 기간 + 단위 + 이벤트」 묶음을 관리한다.

**하는 일**
- 시나리오 조회 / 무작위 선택
- 시나리오에 속한 종목을 `A / B / C / D` 라벨로 매핑 (게임 중 실제 종목명 은닉)
- 시나리오에 붙은 뉴스 이벤트 목록 조회

**하지 않는 일**
- 시나리오 자동 생성 (초기에는 수동 INSERT 또는 간단한 관리 커맨드로 충분)

**완료 판정**
- 시나리오 ID를 주면 캔들 배열 N개 + 이벤트 목록이 나온다

---

### M4. 게임 엔진 `game-engine` ★ 핵심

**책임** — 한 판의 진행 전체. 화면과 네트워크를 모른다.

**하는 일**

```
GameSession
  ├ 상태: tickIndex, players(Map), scenario, candles[][], status
  ├ start()          : 초기 자산 배분, 0틱 세팅
  ├ tick()           : 아래 순서대로 수행
  │    1. tickIndex++
  │    2. 각 종목 현재가 갱신 (해당 틱 캔들 종가)
  │    3. 전 플레이어 × 전 포지션 청산 판정 → 청산 처리
  │    4. 전 플레이어 총자산 재계산
  │    5. 이벤트 발생 틱이면 뉴스 목록에 추가
  │    6. 변경 사항을 TickResult로 반환
  ├ submitOrder(order): 주문 검증 → 체결 or 거부, OrderResult 반환
  └ finish()         : 전 포지션 강제 청산, 순위 산출, GameResult 반환
```

**설계 제약**
- `GameSession`은 **Spring 빈이 아니라 일반 객체**입니다. 방 하나당 하나씩 생성됩니다
- `tick()`과 `submitOrder()`는 **같은 스레드에서 직렬로** 호출되어야 합니다 (§1.6)
- 이 모듈은 WebSocket, DB, Spring 어노테이션을 참조하지 않습니다

**하지 않는 일**
- 네트워크 송수신
- DB 접근
- 스케줄링 (누가 `tick()`을 부를지는 M6의 책임)

**완료 판정**
- **화면 없이** JUnit 테스트에서 `start() → tick() × 240 → finish()`가 돌아간다
- 하드코딩한 주문 시퀀스로 최종 자산과 순위가 계산된다
- 청산이 발생하는 케이스와 발생하지 않는 케이스가 각각 테스트로 검증된다

> **이 모듈이 완성되면 프로젝트의 기술적 불확실성은 대부분 해소됩니다.**

---

### M5. 포지션 · 체결 · 청산 `position`

**책임** — 배율이 걸린 포지션의 진입, 평가, 청산 계산. M4 내부에서 쓰이는 도메인 로직.

**데이터 모양 (러프)**

```
Position
  symbolLabel     // "A"
  side            // LONG (SHORT는 필드만 두고 미구현)
  entryPrice
  quantity
  margin          // 증거금
  leverage        // 1 ~ 10
  liquidationPrice
```

```
PlayerState
  userId, nickname
  cash
  positions: Map<symbolLabel, Position>
  tradeCount
  liquidatedCount
```

**계산 규칙 (전부 조정 가능한 값)**

```
진입                                          (반올림 규칙은 §1.5)
  quantity          = floor( (margin × leverage) / entryPrice )
  actualMargin      = (quantity × entryPrice) / leverage
  liquidationPrice  = entryPrice × (1 − 1/leverage)     scale 4, DOWN
  수수료             = quantity × entryPrice × FEE_RATE   scale 4, UP
  cash             -= actualMargin + 수수료
                                              (FEE_RATE 기본 0.0015)

평가
  미실현손익  = (현재가 − entryPrice) × quantity
  포지션가치  = actualMargin + 미실현손익
  총자산      = cash + Σ 포지션가치

정상 청산(매도)
  cash += actualMargin + 미실현손익
  cash -= min(수수료, cash)
  수수료 = quantity × 현재가 × FEE_RATE                   scale 4, UP

강제 청산
  현재가 ≤ liquidationPrice 이면 포지션 제거 (등호 포함)
  actualMargin 전액 소멸
  cash -= min(수수료, cash)                               ← 사유 불문 1회 (§1.5)

판 종료 시 정리
  정상 청산(매도)과 똑같이 처리한다. 면제하지 않는다 (§1.5 수수료 정책)
```

**잔고 불변식 — `cash` 는 어떤 경로로도 음수가 되지 않는다**

> **수수료는 잔여 현금을 초과해 부과하지 않습니다. `cash` 는 항상 0 이상입니다.**

- 매도·강제 청산·판 종료 정리 전부에 적용됩니다. 구현은 `PlayerState.chargeFee()` 한 곳입니다
- 이유
  - **증거금 전액 소멸이 이미 최대 페널티입니다.** 그 위에 더 빼는 것은 페널티 강화가
    아니라 계산의 부산물입니다
  - 총자산이 음수가 되면 순위표에 수익률 −107% 같은 값이 떠서 버그로 보입니다
  - 「청산돼도 나머지 현금은 살아 있다」는 증거금 격리가 깨집니다. 격리를 넣은 이유가
    터져도 재기 가능하게 만드는 것인데, 잔고가 음수면 남은 시간이 관전이 됩니다
  - 청산 수수료는 명목가치 기준이라 비싼 종목일수록 손해가 커집니다.
    플레이어가 통제할 수도 예측할 수도 없는 차이입니다
- 이것은 §1.5 의 「플레이어에게 유리하게」를 게임 규칙에 적용한 것이 **아닙니다.**
  없는 돈을 빼지 않는다는 것뿐입니다

**배율 상한**

| 모드 | 선택 가능 배율 |
|---|---|
| 일봉 | 1, 2, 3 |
| 분봉 | 1, 3, 5, 10 |

**주문 거부 조건**
- `cash < actualMargin + 수수료`
- `quantity == 0` (증거금이 1주 값에 못 미침) — 사유는 「증거금 부족」
- 배율이 해당 모드의 허용 목록에 없음
- 같은 종목에 이미 포지션 보유 (1차 구현: 종목당 포지션 1개로 제한)
- `margin ≤ 0`

**완료 판정**
- 위 계산 규칙이 전부 단위 테스트로 검증된다
- 경계값(정확히 청산가일 때, 잔고가 딱 맞을 때)이 테스트에 포함된다

**메모**
- 「종목당 포지션 1개」 제한은 구현 단순화를 위한 것입니다. 평단가 계산과 부분 청산을 피할 수 있습니다. 나중에 풀 수 있습니다.

---

### M6. 방 관리 `room`

**책임** — 방 생성, 입퇴장, 게임 시작, 틱 루프 구동, 세션 생명주기.

**하는 일**
- 방 생성 (모드, 인원, 시나리오, 판 길이 설정)
- 대기 중인 방 목록 제공
- 입장 / 퇴장 / 준비 상태 관리
- 전원 준비되면 `GameSession` 생성 후 **1초 주기 스케줄러 시작**
- 매 틱마다 `session.tick()` 호출 → 결과를 M7에 전달
- 주문 요청을 큐에 넣고 틱 루프와 같은 흐름에서 처리
- 종료 시 스케줄러 정지, `finish()` 호출, 결과를 M8에 전달

**하지 않는 일**
- 게임 규칙 계산 (M4/M5의 역할)
- 메시지 직렬화 (M7의 역할)

**완료 판정**
- 방 하나를 만들고 시작하면 240틱이 자동으로 흐르고 정확히 멈춘다
- 방 두 개가 동시에 돌아도 서로 간섭하지 않는다

**메모**
- 스케줄러는 방마다 하나. `ScheduledExecutorService` 기반을 권장합니다
- 주문 큐와 틱을 같은 executor에서 처리하면 §1.6이 자동으로 지켜집니다

---

### M7. 실시간 통신 `realtime`

**책임** — WebSocket 연결 관리와 메시지 송수신.

**클라이언트 → 서버**

| 타입 | 내용 |
|---|---|
| `JOIN` | 방 입장 |
| `READY` | 준비 완료 |
| `ORDER` | `{ symbolLabel, action: BUY/SELL, margin, leverage }` |

**서버 → 클라이언트**

| 타입 | 내용 | 주기 |
|---|---|---|
| `ROOM_STATE` | 참가자 목록, 준비 상태 | 변경 시 |
| `GAME_START` | 종목 라벨, 초기 캔들, 시드머니, 허용 배율 | 시작 시 |
| `TICK` | tickIndex, 종목별 현재가, 남은 시간 | 1초 |
| `PLAYER_STATE` | 내 현금·포지션·총자산 | 1초 (본인에게만) |
| `ORDER_RESULT` | 체결/거부 + 사유 | 주문 처리 후 (본인에게만) |
| `RANKING` | 전원의 순위·총자산·보유 포지션·배율 | 5초 |
| `LIQUIDATED` | 누가 어느 종목에서 청산됐는지 | 발생 시 (전원) |
| `NEWS` | 헤드라인 | 발생 시 (전원 동시) |
| `GAME_END` | 최종 순위, 결과 조회용 ID | 종료 시 |

**규칙**
- `ORDER_RESULT`와 `PLAYER_STATE`는 **본인에게만** 보냅니다
- `RANKING`에는 **모든 참가자의 포지션과 배율이 포함**됩니다 (의도된 설계)
- `NEWS`는 참가자별 지연 없이 **동시 전송**합니다

**완료 판정**
- 브라우저 두 개가 같은 방에 들어가 같은 TICK을 받는다

---

### M8. 결과 · 전적 `result`

**책임** — 판 종료 후 DB 기록과 조회.

**하는 일**
- 종료 시 참가자 결과 + 전체 거래 내역을 **한 트랜잭션에 일괄 INSERT**
- 결과 상세 조회 (순위, 수익률, 거래 내역, 청산 내역, 실제 종목명 공개)
- 사용자별 전적 목록 조회

**하지 않는 일**
- 게임 진행 중 어떤 쓰기도 (§1.2)

**완료 판정**
- 판이 끝나면 결과 화면에서 내 거래를 시간 순으로 볼 수 있다

---

### M9. 봇 `bot`

**책임** — 싱글 모드의 상대.

**하는 일**
- `HoldBot` — 0틱에 전액 매수(배율 1), 끝까지 보유
- `GameSession`에 일반 플레이어와 같은 방식으로 참여

**메모**
- 다른 봇(무작위 매매, 추세추종)은 확장 후보입니다
- 봇은 M4 입장에서 사람과 구분되지 않아야 합니다

**완료 판정**
- 싱글 모드에서 봇과 순위가 나란히 표시된다

---

## 4. 프론트엔드

**2단계로 나눕니다. 1단계를 예쁘게 만들지 않습니다.**

### P0. 테스트 하네스 (M4~M7 검증용)

**목적** — 백엔드가 제대로 도는지 눈으로 확인하는 것. 그 이상 아무것도 아닙니다.

- 방 만들기 / 들어가기 버튼
- TICK으로 들어온 raw JSON을 그대로 화면에 흘리기
- 현재가 텍스트, 주문 폼(종목/증거금/배율 입력 + 버튼)
- 내 현금·포지션·총자산 텍스트
- 순위 목록 텍스트

**금지** — 차트 라이브러리, CSS 프레임워크, 애니메이션, 반응형. 스타일링 자체를 하지 않습니다.

### P1. 실제 화면 (백엔드 검증 완료 후)

| 화면 | 내용 |
|---|---|
| 방 목록 | 대기 중인 방, 모드 표시, 만들기 |
| 대기실 | 참가자, 준비 상태, 시작 |
| 게임 | 캔들 차트, 주문 패널(배율 선택), 순위표(타인 포지션 포함), 뉴스 로그, 남은 시간 |
| 결과 | 순위, 수익률, 거래 지점이 표시된 차트, 실제 종목명 공개 |

**상태 관리** — WebSocket 메시지 타입별로 리듀서를 나누는 정도. 전역 상태 라이브러리는 필요해질 때 도입합니다.

---

## 5. 데이터 모델 (러프)

전체 DDL이 아니라 **무엇이 어디에 저장되는지**만 잡습니다. 컬럼은 구현하면서 채웁니다.

| 테이블 | 담는 것 | 쓰기 시점 |
|---|---|---|
| `users` | 닉네임, (나중에) 계정 | 가입 시 |
| `symbols` | 종목 코드, 이름, 시장 | 수동 |
| `price_candles` | 시세. **PK `(symbol_id, interval, ts)`** | 수집기 배치 |
| `scenarios` | 기간, 단위, 제목 | 수동 |
| `scenario_symbols` | 시나리오 ↔ 종목, 라벨(A/B/C/D) | 수동 |
| `news_events` | 시나리오별 이벤트, 발생 틱, 헤드라인 | 수동 |
| `game_rooms` | 방 정보, 모드, 상태, 시작·종료 시각 | 생성 / 종료 시 |
| `game_participants` | 참가자별 최종 자산, 수익률, 순위, 청산 횟수 | **종료 시** |
| `trades` | 거래 내역 (틱, 종목, 매수/매도, 증거금, 배율, 가격, 청산 여부) | **종료 시 일괄** |

**주의 사항**
- `price_candles`의 PK 순서가 중요합니다. 조회는 항상 「한 종목의 특정 구간」이므로 `symbol_id`가 선두여야 합니다
- 가격 컬럼은 전부 `DECIMAL`
- **현재 상태 테이블은 없습니다** (§1.3)

---

## 6. 개발 순서

각 단계는 **그 단계까지만으로도 동작하는 것**이 있어야 합니다.

| 단계 | 하는 일 | 완료 판정 |
|---|---|---|
| **S0** | 토스 계좌·API 키 발급. 일봉 수동 호출해 CSV 저장. WebSocket 튜토리얼로 브라우저 2개 동기화 확인 | 데이터가 손에 있고, 소켓이 뜬다. **코드는 버린다** |
| **S1** | M1, M2 | 커맨드로 DB에 캔들이 쌓인다 |
| **S2** | M4, M5 | **테스트에서 240틱이 돌고 청산·순위가 나온다** |
| **S3** | M3, M6, M7, P0 | 브라우저에서 혼자 한 판을 끝까지 한다 |
| **S4** | M8, M9 | 결과가 저장되고 봇과 겨룬다 |
| **S5** | 멀티플레이 (M6·M7 확장) | 두 명이 같은 방에서 논다 |
| **S6** | P1, 뉴스 이벤트, 분봉 모드 | 게임처럼 보인다 |
| **S7** | 안정화, 문서, 발표 | **기능 추가 중단** |

**S2가 분기점입니다.** 여기가 안 되면 일정이 아니라 설계를 다시 봐야 합니다.
**S3까지 오면 제출할 결과물이 확보됩니다.**

---

## 7. 용어

| 용어 | 뜻 |
|---|---|
| 틱 (tick) | 게임 시간 최소 단위. 1초 = 캔들 1개 전진 |
| 시나리오 | 한 판에 쓰는 종목 + 기간 + 단위 묶음 |
| 증거금 (margin) | 포지션에 건 돈. 청산되면 이만큼 잃는다 |
| 배율 (leverage) | 증거금 대비 포지션 크기 |
| 청산 (liquidation) | 손실이 증거금을 넘어 포지션이 강제 종료되는 것 |
| 총자산 | 현금 + 모든 포지션 가치. 순위 기준 |
| 라벨 | 게임 중 종목을 가리는 이름 (A/B/C/D) |

---

## 8. 미결정 사항

**아래는 아직 정하지 않았습니다. 관련 코드를 작성할 때는 값을 상수로 빼두고 확인을 구합니다.**

- ~~1분봉을 과거 몇 년까지 받을 수 있는가~~ → **해소.** 일봉 51년(경계 확정),
  1분봉 최소 2년 4개월(경계 미측정). 실측치는 §M1 메모 참조.
  1분봉의 정확한 경계만 아직 모르지만 게임 설계에 영향이 없어 우선순위를 내린다
- 배율 상한(일봉 3 / 분봉 10)이 적절한가 → S3 이후 플레이하며 조정
- 한 판 길이 240틱이 맞는가 → 방 설정으로 뺄지 검토
- 뉴스를 어떻게 생성할 것인가 (자동 / 수동)
- 종목당 포지션 1개 제한을 풀 것인가
- 수수료율 0.0015가 적절한가

---

*v0.1 — 구현하면서 갱신합니다. 규칙(§1)이 바뀌면 반드시 이 문서를 먼저 고칩니다.*
